server:
  port: 8080
  servlet:
    context-path: /weekday

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/weekday?characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: weekday
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true